
#[derive(Default)]
struct GemeOptions {
    pub radius: f32,
    
}
#[derive(Default)]
struct AgentOptions {
    
}
#[derive(Default)]
struct AgentOptions {
    pub view_range: f32,
    pub pos_w: f32,
    pub vel_w: f32,
    pub drag: f32,
    pub max_acc: f32,
    pub weirdness: f32,
    pub s_in_range: usize,
    pub s_vel: f32,
    pub color: f32,
}